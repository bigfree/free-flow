import { FC, ReactElement } from 'react';

const Aside: FC = (): ReactElement => {
    return (
        <div>
            <p>Aside component</p>
        </div>
    );
};

export default Aside;